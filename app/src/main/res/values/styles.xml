<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="AdoroThemeWhite.FlatBtn" parent="Widget.AppCompat.Button.Borderless">
<!--
        <item name="android:background">@drawable/btn_flat</item>
-->
        <item name="android:height">@dimen/cta_btn_height</item>
        <item name="android:textSize">15sp</item>
<!--
        <item name="android:textColor">White</item>
-->
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
